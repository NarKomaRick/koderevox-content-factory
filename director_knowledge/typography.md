# Typography

Use a clear hierarchy: headline, subtitle, callout, label, code, and CTA have different jobs. Prefer fewer readable words to a dense caption wall, and never sacrifice safe zones for decoration.
